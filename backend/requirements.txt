# ── AgriPulse Core Backend (the "Core Engine" box in the architecture diagram) ──
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9          # multipart form / file uploads

# ── Database ──
SQLAlchemy==2.0.35
psycopg2-binary==2.9.9           # PostgreSQL driver
alembic==1.13.2                  # (optional) DB migrations

# ── Validation / settings ──
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0           # validates EmailStr

# ── Auth: JWT + password hashing ──
python-jose[cryptography]==3.3.0 # JWT encode/decode
passlib[bcrypt]==1.7.4           # bcrypt password hashing
bcrypt==4.0.1                    # pinned: passlib 1.7.4 breaks on bcrypt>=4.1

# ── Calling the AI service / weather API ──
httpx==0.27.2
